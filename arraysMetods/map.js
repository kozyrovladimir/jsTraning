const myArr = [5, 6, 58, 41, 12, 94];

const newArr = myArr.map((item) => item *2);

console.log(newArr);

/*
Код выше демонстрирует применение метода map. Метод map принимает внурь callback функцию.
Метод возвращает новый массив на основе того, что возвращает callback.
*/